# This file contains constraints for python dependencies in Composer.
#
# These constraints are enforced only during preparation of the preinstalled
# packages for Composer Airflow images. They are NOT enforced during
# installation of custom python packages (python packages installed by customer
# on top of preinstalled), meaning that customer can technically install
# versions of the packages that do not satisfy constraints specified in this
# file.
#
# SV - constraint(s) to avoid security vulnerabilities.
aiohttp>=3.8.5  # SV
# 10.10.0 has a regression breaking XCom in deferrable K8s operators
# 10.12.0 and 10.12.1 break deferrable mode https://github.com/apache/airflow/issues/60600
apache-airflow-providers-cncf-kubernetes!=10.10.0,!=10.12.0,!=10.12.1
apache-airflow-providers-common-compat>=1.8.0  # unit tests failing for versions prior to 1.8.0
apache-airflow-providers-fab>=3.0.0  # help pip to properly resolve dependencies
apache-beam>=2.70.0  # help pip to properly resolve dependencies
click<8.3.0  # 8.3.0 version is not compatible with Airflow
dbt-adapters>=1.22.5  # help pip to properly resolve dependencies
dbt-bigquery>=1.11.0  # help pip to properly resolve dependencies
dbt-core>=1.11.2  # help pip to properly resolve dependencies
fastapi<=0.118.2  # later versions are not compatible with Airflow
Flask-AppBuilder>=4.6.3  # help pip to properly resolve dependencies
firebase-admin>=7.0.0
flower>=2.0.0  # SV
google-cloud-aiplatform>=1.132.0  # help pip to properly resolve dependencies
google-cloud-asset>=4.1.0  # help pip to properly resolve dependencies
google-cloud-bigquery-storage>=2.36.0  # help pip to properly resolve dependencies
google-cloud-datacatalog-lineage-producer-client>=0.2.2  # help pip to properly resolve dependencies
google-cloud-datastore>=2.21.0  # help pip to properly resolve dependencies
google-cloud-documentai>=3.7.0  # help pip to properly resolve dependencies
google-cloud-filestore>=1.14.0  # help pip to properly resolve dependencies
google-cloud-firestore>=2.23.0  # help pip to properly resolve dependencies
pandas<=2.1.4  # some unit tests are failing for higher versions
pip==23.2.1
pygments>2.15.0  # SV
# TODO: remove <9.0.0 in Airflow 3.1.3+ https://github.com/apache/airflow/pull/58166
pytest!=8.4.0,<9.0.0  # https://github.com/pytest-dev/pytest/issues/13477
setuptools>=78.1.1  # SV
sqlalchemy-spanner!=1.12.0  # https://github.com/googleapis/python-spanner-sqlalchemy/issues/682
structlog<25.5.0  # 25.5.0 is incompatible with Airflow
tensorflow>=2.20.0  # help pip to properly resolve dependencies
urllib3>=2.6.0 # SV
virtualenv>=20.24.0  # SV
werkzeug>=2.2.3  # SV
